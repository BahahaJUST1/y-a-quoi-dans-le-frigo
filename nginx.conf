# ====================================================== #
# IF THE PROJECT IS ACCESSIBLE FOR EVERYBODY ON INTERNET #
# ====================================================== #

# --------------------------------------------------- #
# ADD THIS BLOCK IF NEED TO GENERATE SSL CERTIFICATES #
# --------------------------------------------------- #

# server {
#     listen 80;
#     server_name y-a-quoi-dans-le-frigo.fr y-a-quoi-dans-le-frigo.com y-a-quoi-dans-le-frigo.online;

#     location /.well-known/acme-challenge/ {
#         root /var/www/html;
#     }
# }


# ---------------------------------------------------- #
# OR ADD THIS BLOCK IF CERTIFICATES HAS BEEN GENERATED #
# ---------------------------------------------------- #

# server {
#     listen 80;
#     server_name y-a-quoi-dans-le-frigo.fr y-a-quoi-dans-le-frigo.com y-a-quoi-dans-le-frigo.online;

#     # redirect every HTTP traffic to HTTPS
#     return 301 https://$host$request_uri;
# }

# server {
#     listen 443 ssl;
#     server_name y-a-quoi-dans-le-frigo.fr y-a-quoi-dans-le-frigo.com y-a-quoi-dans-le-frigo.online;

#     # SSL certificates
#     ssl_certificate /etc/letsencrypt/live/y-a-quoi-dans-le-frigo.fr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/y-a-quoi-dans-le-frigo.fr/privkey.pem;

#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;

#     # backend (API) routes
#     location /api {
#         proxy_pass http://backend:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # frontend routes
#     location / {
#         proxy_pass http://frontend:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#         # Support for React Router
#         proxy_intercept_errors on;
#         error_page 404 = @react_router;
#     }

#     # Fallback for React Router client-side routing
#     location @react_router {
#         rewrite .* /index.html break;
#         proxy_pass http://frontend:80;
#     }
# }




# ======================================================== #
# IF THE PROJECT IS ACCESSIBLE ONLY FOR THOSE ON SAME WIFI #
# ======================================================== #

server {
    listen 80;
    server_name 192.168.1.92;

    # backend (API) routes
    location /api {
        proxy_pass http://backend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # frontend routes
    location / {
        proxy_pass http://frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Support React Router
        proxy_intercept_errors on;
        error_page 404 = @react_router;
    }

    location @react_router {
        rewrite .* /index.html break;
        proxy_pass http://frontend:80;
    }
}